services:
  backend:
    build:
      context: ../..
      dockerfile: backend/Dockerfile.prod
    ports:
      - "8088:8088"
    env_file: .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
